# Example: PVC only (no VM or Secret)
_params = {
    oxr = {
        spec = {
            name = "pvc-only"
            namespace = "storage"
            pvcName = "data-disk-1"
            imageNamespace = "harvester-public"
            imageId = "image-ubuntu-22.04"
            storage = "50Gi"
            storageClass = "longhorn"

            # Enable only PVC
            enablePvc = True
            enableSecret = False
            enableVm = False
        }
    }
}
